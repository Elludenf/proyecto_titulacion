<?php
/* 
 * Generated by CRUDigniter v2.3 Beta 
 * www.crudigniter.com
 */
 
class Permiso_model extends CI_Model
{
    function __construct()
    {
        parent::__construct();
    }
    
    /*
     * Get permiso by zpermcodigo
     */
    function get_permiso($perm_codigo)
    {
        return $this->db->get_where('permisos',array('perm_codigo'=>$perm_codigo))->row_array();
    }
    private $table = "permisos";
    function count()
    {
        return $this->db->count_all_results($this->table);
    }

    /*
     * Get all permisos
     */
    function get_all_permisos($limit = 5)
    {
        $offset = $this->uri->segment(3);
        return $this->db->limit($limit, $offset)
            ->get('permisos')->result_array();
    }
    
    /*
     * function to add new permiso
     */
    function add_permiso($params)
    {
        $this->db->insert('permisos',$params);
        /*
        $this->db->select_max('zpermcodigo');
        $result= $this->db->get('permisos')->row_array();
        return $this->db->$result;
        */
        //return $this->db->insert_id();
    }
    
    /*
     * function to update permiso
     */
    function update_permiso($perm_codigo,$params)
    {
        $this->db->where('perm_codigo',$perm_codigo);
        $response = $this->db->update('permisos',$params);
        if($response)
        {
            return "permiso updated successfully";
        }
        else
        {
            return "Error occuring while updating permiso";
        }
    }
    
    /*
     * function to delete permiso
     */
    function delete_permiso($perm_codigo)
    {
        $response = $this->db->delete('permisos',array('perm_codigo'=>$perm_codigo));
        if($response)
        {
            return "permiso deleted successfully";
        }
        else
        {
            return "Error occuring while deleting permiso";
        }
    }
}
